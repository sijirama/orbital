import SideBar from '../layout/SideBar'

export default function Dashboard() {
    return (
        <SideBar>
            <main>
                <div className="font-semibold text-md">Dashboard</div>
            </main>
        </SideBar>
    )
}
